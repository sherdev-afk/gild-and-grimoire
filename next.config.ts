// next.config.ts

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['gildandgrimoire.com', 'gildandgrimoire.printify.me'],
  },
  reactStrictMode: true,
  };

export default nextConfig;